/** One-shot launcher notice rendered after a surface handoff resumes the TUI. */
export const STARTUP_NOTICE_ENV = 'DSH_CODE_STARTUP_NOTICE'

/** One-shot marker that keeps an empty Web-returned Session disposable. */
export const DISCARD_EMPTY_RESUME_ENV = 'DSH_CODE_DISCARD_EMPTY_RESUME'
